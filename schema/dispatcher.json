{
  "jupyter.lab.shortcuts": [],
  "title": "notebook-metrics",
  "description": "notebook-metrics settings, ignored if page config is set to override",
  "type": "object",
  "properties": {
    "disabled": {
      "title": "Emit nothing",
      "description": "Ignored if page config overrides are present",
      "default": true,
      "type": "boolean"
    },
    "anonymous": {
      "title": "Anonymous data emissions only",
      "description": "Ignored if page config overrides are present or if emissions are disabled",
      "default": true,
      "type": "boolean"
    },
    "sensitivity": {
      "title": "Sensitivity level of emissions",
      "description": "Ignored if page config overrides are present or if emissions are disabled",
      "enum": ["high", "moderate", "low"],
      "default": "low"
    },
    "excluded": {
      "title": "Excluded emissions",
      "description": "Full schema URLs to exclude, ignored if page config overrides are present or if emissions are disabled",
      "type": "object",
      "properties": {
        "https://schema.notebook.link/metrics/command-executed/v1": {
          "type": "boolean",
          "default": false,
          "title": "Exclude command executed emissions"
        },
        "https://schema.notebook.link/metrics/current-changed/v1": {
          "type": "boolean",
          "default": false,
          "title": "Exclude current activity changed emissions"
        },
        "https://schema.notebook.link/metrics/jupyter-error/v1": {
          "type": "boolean",
          "default": false,
          "title": "Exclude Jupyter output error emissions"
        },
        "https://schema.notebook.link/metrics/runtime-error/v1": {
          "type": "boolean",
          "default": false,
          "title": "Exclude frontend runtime error emissions"
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
