$id: https://quantstack.net/schema/metrics/command-executed/v1
version: 1
title: command executed metrics emission
description: |
  a command execution metrics emission
type: object
properties:
  metrics:
    type: object
    properties:
      command:
        type: string
      args:
        type: object
        additionalProperties: true
      label:
        optional: true
        type: string
    required:
      - command
required:
  - metrics
